package nat20.kamppisserver.api

/**
 * REST controller for Message.
 *
 * Lacking GET all, POST, PUT, and DELETE methods. New chat is POSTed
 * with every new Match. A chat is PUT every time there is a new Message.
 * A chat can be DELETEd by reporting or undoing the Match.
 */
class ChatController {
}